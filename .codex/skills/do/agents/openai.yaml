interface:
  display_name: "Do"
  short_description: "Use when the user wants to implement a feature with full lifecycle management."
  default_prompt: "Use $do to implement a feature with full lifecycle management."
