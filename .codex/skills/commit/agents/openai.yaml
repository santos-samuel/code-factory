interface:
  display_name: "Commit"
  short_description: "Use when the user wants to commit current changes, create a git commit,."
  default_prompt: "Use $commit to commit current changes, create a git commit,."
