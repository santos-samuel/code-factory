interface:
  display_name: "Rfc"
  short_description: "Use when the user wants to write an RFC (Request for Comments), design docume..."
  default_prompt: "Use $rfc to write an RFC (Request for Comments), design document,."
